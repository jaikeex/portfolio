import styled from '@emotion/styled';

export const Section = styled('section')({
  minHeight: '100vh',
  width: '100%',
  padding: '6rem 1rem',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center'
});
